(1) The memory allocations can be up to 128 bytes in size. Within the 2048 bytes we've allocated, this space gets used up very quickly, and the result of 23 out of 500,000 successful allocations makes sense because there ends up being many small chunks of free space that aren't coalesced together to support a new 128-byte request, even though the total available space should be enough.